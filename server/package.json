{
  "name": "logbook-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Optional self-hosted sync and notifications for Logbook",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  }
}
